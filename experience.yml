version: 1
project: Vulnerability Assessment Platform Interface
steps:
  - name: Install dependencies
    command: npm install
    description: Install Express, Multer, and other dependencies required for the demo service.
  - name: Start the demo service
    command: npm start
    description: Launch the front-end page and demo APIs on local port 5173.
